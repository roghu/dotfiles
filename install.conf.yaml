- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/.config/git

- link: {
  ~/.config/git/config: git/config
}

- shell:
  - [git submodule update --init --recursive, Installing submodules]
